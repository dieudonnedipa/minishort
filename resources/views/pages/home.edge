<!DOCTYPE html>
<html>
<head>
  <title>MiniShort - Accueil</title>
  <style>
    body { display: flex; font-family: Arial; margin: 0; }
    .sidebar { width: 300px; background: #f4f4f4; padding: 20px; border-right: 1px solid #ddd; height: 100vh; overflow-y: auto; }
    .content { flex: 1; padding: 20px; }
    input, button { width: 100%; padding: 10px; margin: 10px 0; }
    .short-url { color: #007bff; font-weight: bold; }
    .card { margin-bottom:15px; padding:10px; background:#fff; border-radius:5px; }
  </style>
</head>
<body>
  <div class="sidebar">
    <h2>📌 Mes URLs</h2>
    @each(url in urls)
      <div class="card">
        <p><b>Original:</b> {{ url.originalUrl }}</p>
        <p><a class="short-url" href="/{{ url.shortCode }}" target="_blank">{{ url.shortCode }}</a></p>
        <img src="{{ url.qrCode }}" alt="QR" style="max-width:100px;">
        <form action="/urls/{{ url.id }}/delete" method="POST">
          {{ csrfField() }}
          <button type="submit" style="background:red; color:white;">Supprimer</button>
        </form>
        <form action="/urls/{{ url.id }}/update" method="POST">
          {{ csrfField() }}
          <input type="text" name="originalUrl" value="{{ url.originalUrl }}" placeholder="Nouvelle URL">
          <input type="text" name="shortCode" value="{{ url.shortCode }}" placeholder="Nouveau shortCode">
          <button type="submit">Mettre à jour</button>
        </form>
      </div>
    @endeach
  </div>

  <div class="content">
    <h1>🔗 Raccourcisseur d'URL</h1>
    <form action="/shorten" method="POST">
      {{ csrfField() }}
      <input type="url" name="originalUrl" placeholder="Entrez votre URL ici..." required>
      <input type="text" name="shortCode" placeholder="Entrer un shortCode (optionnel)">
      <button type="submit">Envoyer l'URL</button>
    </form>
  </div>
</body>
</html>